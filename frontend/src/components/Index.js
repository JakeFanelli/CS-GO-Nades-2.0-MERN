import React from "react";

const Index = props => (
  <div className="container">
    <h2>Home</h2>
    <h5 className="welcomeText">
      Learn how to throw every grenade on all competetive maps.
    </h5>
    <p className="subText">Create an account to do x, y, and z.</p>
  </div>
);

export default Index;
