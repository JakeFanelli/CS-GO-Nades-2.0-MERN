import React from "react";

const Index = props => (
  <div className="container">
    <h5 className="welcomeText">
      Learn how to throw every grenade on all competetive maps and recoil
      patterns
    </h5>
    <p className="subText">
      Create an account to submit nades, and vote on user submitted nades
    </p>
  </div>
);

export default Index;
